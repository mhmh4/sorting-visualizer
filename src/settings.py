BLOCK_SIZE = 25

# Screen dimensions
WIDTH = 800
HEIGHT = 200

# Every rectangle will have at least a 1 block gap around its perimeter
WIDTH += BLOCK_SIZE
HEIGHT += BLOCK_SIZE

# For larger screen dimensions, decrease the delay time
DELAY_MS = 200 # in milliseconds
